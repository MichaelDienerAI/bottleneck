{
  "name": "bottleneck",
  "version": "0.1.0",
  "description": "Bottleneck. Constraint-first agentic job search. Discovery is subordinated to proof-packet throughput.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "verify": "node src/verify.js",
    "scan": "node src/scan.js",
    "slots": "node src/ledger.js --slots",
    "report": "node src/ledger.js --report",
    "test": "node src/gates.test.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
